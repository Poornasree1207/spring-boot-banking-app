server: 
  port:8080

  spring:
    application:
        name:banking-backend

  datasource:
    url:jdbc:h2:mem:bankdb
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console
      